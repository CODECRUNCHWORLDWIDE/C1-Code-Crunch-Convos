"""exercise-02-repl-explorer.py

Goal
----
Get comfortable driving the Python REPL like a pocket calculator. You will
open the interactive interpreter, evaluate eight different expressions,
and paste the results back into this file as comments.

How to do this exercise
-----------------------
1. Open a terminal and activate your Week 1 virtual environment.
2. Start the REPL with::

       python

   (or ``python3`` if that's what works on your system).

3. For each numbered expression below, type the expression into the REPL,
   press Enter, copy the result, and paste it on the ``# answer:`` line.
4. When you have filled in all eight answers, save this file. There is no
   ``main()`` to run — this exercise lives entirely in the docstring and
   the comments below.

Exit the REPL when you're done with ``exit()`` or Ctrl+D.

Estimated time
--------------
About 15 minutes.

Hints
-----
* The REPL prints the result of every expression automatically. You do not
  need to call ``print``.
* ``**`` is the exponent operator (``2 ** 10`` means "2 to the 10th").
* ``//`` is integer division, ``%`` is modulo (remainder).
* Strings can be added together with ``+`` and repeated with ``*``.
* The built-in ``len()`` returns the number of characters in a string.
"""

# ----------------------------------------------------------------------
# Expressions to evaluate. Replace each ``# answer:`` placeholder with the
# value the REPL printed back to you.
# ----------------------------------------------------------------------

# 1) Type:  2 + 3 * 4
# answer:

# 2) Type:  2 ** 10
# answer:

# 3) Type:  17 // 5
# answer:

# 4) Type:  17 % 5
# answer:

# 5) Type:  "code" + " " + "crunch"
# answer:

# 6) Type:  "ha" * 4
# answer:

# 7) Type:  len("Hello, world!")
# answer:

# 8) Type:  round(3.14159, 2)
# answer:


# When you are done, scroll up and double-check each answer.
# (No code to run. This file is a paper-and-pencil exercise.)
