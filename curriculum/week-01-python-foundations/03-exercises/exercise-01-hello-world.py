"""exercise-01-hello-world.py

Goal
----
Run your very first Python script. The script should print the classic
greeting "Hello, world!" to your terminal.

Expected output
---------------
When you run this file with::

    python exercise-01-hello-world.py

you should see exactly the following line printed (no extra spaces, no
quotes)::

    Hello, world!

Hints
-----
* Use the built-in :func:`print` function.
* Strings in Python can be wrapped in single quotes ('like this') or
  double quotes ("like this"). Either is fine.
* Python is case-sensitive — ``print`` works, ``Print`` does not.

Estimated time
--------------
Under 5 minutes.
"""


def main() -> None:
    """Print the canonical first-program greeting."""
    # TODO: replace the ``pass`` statement below with a call to ``print``
    # so that the program prints exactly:  Hello, world!
    pass


if __name__ == "__main__":
    main()
